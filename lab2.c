#include <stdio.h>
#include <stdlib.h>

int main()
{
    double side;
    puts("what is the length of the side?");
    scanf("%f", &side);
    printf("P is %f, S is %f", (side + side) * 2, side * side);
    return 0;
}
//варіант6
